{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "_comment": "Project-specific permission additions only. Baseline (npm/git/node/curl/gh/ssh/etc) lives in ~/.claude/settings.json — do not duplicate it here. Add patterns specific to THIS project below.",
  "permissions": {
    "allow": []
  }
}
